services:
  - type: web
    name: sports-tracker
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: NTFY_TOPIC
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: PORT
        value: 10000
